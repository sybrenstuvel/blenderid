| {% extends 'layout.html' %}
| {% load gravatar %}
| {% block page_title %}
| {% if user.full_name %}
| {{user.full_name}}'s
| {% else %}
| Your
| {% endif %}
| Profile
| {% endblock %}
| {% block body %}
.row.id
	.col-md-6
		.box.id__profile-details
			img.img-circle.id__profile-details-avatar(src='{% gravatar_url user.email 120 %}', title='Gravatar Profile Picture')
			| {% if user.full_name %}
			h1.id__profile-details-name
				| {{ user.full_name }}
			| {% else %}
			h1.danger Your full name is not known
			p
				a.btn.btn-danger(href="{% url 'bid_main:profile' %}")
					i.fa.fa-edit
					| Edit Profile
			| {% endif %}
			p.id__profile-details-email
				| {{ user.email }}
			p
				| {% if user.full_name %}
				a.btn.btn-default(href="{% url 'bid_main:profile' %}")
					i.fa.fa-edit
					|  Edit Profile
				| {% endif %}
				a.btn.btn-default(href="{% url 'bid_main:password_change' %}")
					i.fa.fa-edit
					|  Change Password
			| {% if not user.confirmed_at and config.SECURITY_CONFIRMABLE %}
			p
				a.btn.btn-warning(href="-- url_for_security('send_confirmation') --")
					i.fa.fa-envelope
					|  Please confirm your email address
			| {% endif %}
			p
				a.btn.btn-default(href="{% url 'bid_main:logout' %}")
					i.fa.fa-sign-out
					|  Log out
				a.btn.btn-default(href="{% url 'bid_main:switch_user' %}")
					i.fa.fa-sign-out
					|  Log in as someone else

		// id__profile-details
		| {% if 'bfct_trainer' in user.role_names %}
		.box.id__profile-certification
			i.fa.fa-graduation-cap.backicon
			h3.id__profile-certification-title Blender Foundation Certified Trainer
			p
				| You're an official
				span.info  Blender Foundation Certified Trainer
				br
				| More details about your
				a(href='https://www.blender.org/certification')  certification
				|         {% if 'bfct_board' in user.role_names %}
				hr.width-full
			p.info
				i.fa.fa-graduation-cap
				|  BFCT Board Member{% if 'bfct_manager' in user.role_names %} &amp; Manager{% endif %}
			|         {% endif %}
			p
		|     {% endif %}
	// col-md-6
	.col-md-6
		.box.id__profile-subscriptions
			i.bf-cloud.backicon
			h3.id__profile-subscriptions-title Blender Cloud
			p
				| Visit your
				a(href='https://cloud.blender.org/settings/billing')  preferences
				|  for more details
		.box.id__profile-subscriptions
			i.bf-network.backicon
			h3.id__profile-subscriptions-title Blender Network
			p
				| Visit your
				a(href='https://www.blendernetwork.org/profile/edit')  profile settings
				|  for subscription details
		//
			<div class="box padding-top-1">
			<h3>Blender Conference</h3>
			<p></p>
			</div> <!-- box
| {% endblock %}
